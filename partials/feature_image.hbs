{{!- 
    Parameters:
    image_size [] xs | s | m | w | xl ] (when not set a responsive srcset is used)
    srcset is only used for larger images, since these are the only sizes where having a smaler version on mobiles makes a signficant difference

--}}
<figure class="feature-image {{#match image_format "wide"}}kg-width-wide{{/match}}">
    <img  
        {{#if image_size}}
            src='{{img_url feature_image size=image_size}}'
        {{else}}
            src='{{img_url feature_image size="m"}}'
        {{/if}}
        {{#match image_size "!=" "xs" }}
        {{#match image_size "!=" "s" }}
            srcset='{{img_url feature_image size="s"}} 400w,
            {{img_url feature_image size="m" }} 800w,
            {{img_url feature_image size="l" }} 1600w'
        {{/match}}
        {{/match}}
        alt="{{#if feature_image_alt}}{{feature_image_alt}}{{else}}{{title}}{{/if}}"
    >
    {{#if feature_image_caption}}
        <figcaption class="feature_image_caption">{{feature_image_caption}}</figcaption>
    {{/if}}
</figure>
