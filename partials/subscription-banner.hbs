{{!--
    Parameters :
    mini [ true, false]
    location [ post, home ]
--}}
<aside class="subscription-banner {{#if mini}}mini{{/if}}">
    {{^if mini}}
        <h2 class="subscription-banner-heading">
            {{#match location "post" }}
                {{#if @custom.post_subscription_banner_heading}}
                    {{@custom.post_subscription_banner_heading}}
                {{else}}
                    {{@site.title}}
                {{/if}}
            {{else}}
                {{#if @custom.home_page_subscription_banner_heading}}
                    {{@custom.home_page_subscription_banner_heading}}
                {{else}}
                    {{@site.title}}
                {{/if}}
            {{/match}}
        </h2>
        <p class="subscription-banner-description">
            {{#match location "post"}}
                {{#if @custom.post_subscription_banner_description}}
                    {{@custom.post_subscription_banner_description}}
                {{else}}
                    {{@site.description}}
                {{/if}}
            {{else}}
                {{#if @custom.home_page_subscription_banner_description}}
                    {{@custom.home_page_subscription_banner_description}}
                {{else}}
                    {{@site.description}}
                {{/if}}
            {{/match}}
        </p>
    {{/if}}
    <form class="subscription-banner-form" data-members-form>
        <input class="form-input" id="{{email_field_id}}" type="email" placeholder="your.email@example.com" required data-members-email>
        <button class="button icon-button" type="submit" aria-label="Subscribe">
            <span class="text">Subscribe</span> 
            <span class="loading-icon" >
                {{> "icons/loading"}}
            </span>
            <span class="success-icon"> 
                {{> "icons/success"}}
            </span>
            <span class="error-icon"> 
                {{> "icons/error"}}
            </span>
        </button>
        <p class="error-text" data-members-error></p>
    </form>
</aside>
